# Fetched fresh on every deploy by scripts/fetch-tickers.sh (see that file
# and js/sec.js) — not committed, so it can never go stale.
/data/

# Local Claude Code agent worktree checkouts — machine-local tooling state,
# not project content.
/.claude/
