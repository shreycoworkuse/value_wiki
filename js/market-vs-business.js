// Placeholder — replaced by the Market vs Business screen (book/FCF value
// trend vs current price snapshot) currently being built. Kept here so
// js/money-flow.js's import graph stays valid while that work lands.
export function renderMarketVsBusiness(container) {
  container.innerHTML = `<p class="money-flow-placeholder">Market vs business is being built.</p>`;
}
