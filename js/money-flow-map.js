// Placeholder — replaced by the Business Map screen (animated flow diagram
// + click-to-trace drill-down) currently being built. Kept here so
// js/money-flow.js's import graph stays valid while that work lands.
export function renderBusinessMap(container) {
  container.innerHTML = `<p class="money-flow-placeholder">Business map is being built.</p>`;
}
