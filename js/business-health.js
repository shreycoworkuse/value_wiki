// Placeholder — replaced by the Business Health screen (KPI trend grid +
// growth/ROIC bubble chart) currently being built. Kept here so
// js/money-flow.js's import graph stays valid while that work lands.
export function renderBusinessHealth(container) {
  container.innerHTML = `<p class="money-flow-placeholder">Business health is being built.</p>`;
}
